import java.util.*;
public class reverse {
	public static void main(String[] args) {
		String str;
		Scanner sc=new Scanner(System.in);
		System.out.println("Enter the word");
		str=sc.nextLine();
		StringBuffer buf = new StringBuffer(str);
		System.out.print(str+"|"+buf.reverse());

	}
}
